

window.Code_API = Master.extend({
    url:apiHost + "codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url: apiHost + "codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:apiHost + "codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url: apiHost + "codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:apiHost + "codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:apiHost + "codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:apiHost + "codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:apiHost + "codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});


window.Code_API = Master.extend({
    url:apiHost + "codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url: apiHost + "codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:apiHost + "codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url: apiHost + "codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:apiHost + "codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:apiHost + "codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:apiHost + "codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:apiHost + "codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});


window.Code_API = Master.extend({
    url:apiHost + "codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url: apiHost + "codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:apiHost + "codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url: apiHost + "codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:apiHost + "codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:apiHost + "codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:apiHost + "codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:apiHost + "codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});


window.Code_API = Master.extend({
    url:apiHost + "codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url: apiHost + "codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:apiHost + "codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url: apiHost + "codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:apiHost + "codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:apiHost + "codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:apiHost + "codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:apiHost + "codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});


window.Code_API = Master.extend({
    url:apiHost + "codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url: apiHost + "codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:apiHost + "codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url: apiHost + "codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:apiHost + "codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:apiHost + "codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:apiHost + "codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:apiHost + "codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});


window.Code_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});


window.Code_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});


window.Code_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});


window.Code_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});

window.Code_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.Code_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.Code_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.Code_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:"http://localhost.ontologydriven/api/codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.Code_API = Master.extend({
    url:apiHost + "codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url: apiHost + "codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:apiHost + "codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url: apiHost + "codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:apiHost + "codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:apiHost + "codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:apiHost + "codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:apiHost + "codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.Code_API = Master.extend({
    url:apiHost + "codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url: apiHost + "codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:apiHost + "codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url: apiHost + "codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:apiHost + "codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:apiHost + "codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:apiHost + "codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:apiHost + "codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.Code_API = Master.extend({
    url:apiHost + "codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url: apiHost + "codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:apiHost + "codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url: apiHost + "codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:apiHost + "codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:apiHost + "codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:apiHost + "codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:apiHost + "codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.Code_API = Master.extend({
    url:apiHost + "codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url: apiHost + "codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:apiHost + "codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url: apiHost + "codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:apiHost + "codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:apiHost + "codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:apiHost + "codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:apiHost + "codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.Code_API = Master.extend({
    url:apiHost + "codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url: apiHost + "codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:apiHost + "codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url: apiHost + "codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:apiHost + "codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:apiHost + "codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:apiHost + "codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:apiHost + "codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.Code_API = Master.extend({
    url:apiHost + "codegeneration/generate/code/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_DataBase = Master.extend({
    url: apiHost + "codegeneration/generate/code/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/generate/code/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Logic = Master.extend({
    url:apiHost + "codegeneration/generate/code/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Code_Website = Master.extend({
    url: apiHost + "codegeneration/generate/code/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"code":"",
    	"repository":""
    }
});

window.Deployment_API = Master.extend({
    url:apiHost + "codegeneration/deploy/api",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_DataBase = Master.extend({
    url:apiHost + "codegeneration/deploy/database",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_FrontEnd = Master.extend({
    url:apiHost + "codegeneration/deploy/frontend",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Logic = Master.extend({
    url:apiHost + "codegeneration/deploy/logic",
    
    initialize: function(model) {
    	this.url += "?ontologyID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Deployment_Website = Master.extend({
    url:apiHost + "codegeneration/deploy/website",
    
    initialize: function(model) {
    	this.url += "?websiteID=" + model.id;
    },
    defaults:{
    	"repository":""
    }
});

window.Website = Master.extend({
	urlRoot : apiHost + "web/websites",
	
	defaults : {
		"id" : null,
		"name" : "",
	}
});

window.WebsiteCollection = MasterCollection.extend({
	model : Ontology,
	url : apiHost + "web/websites"
});
window.GeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_codegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd"
    },
    
    generateCode:function () {
    	var code_database = new Code_DataBase({id: this.model.id});
    	var code_api = new Code_API({id: this.model.id});
    	var code_logic = new Code_Logic({id: this.model.id});
    	var code_frontend = new Code_FrontEnd({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	
    	var response = "";
    	code_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		if(code_database.get('repository') != null){
    			response += code_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_database" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_database.get('error') != null){
    				if(code_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		if(code_api.get('repository') != null){
    			response += code_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_api" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_api.get('error') != null){
    				if(code_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		if(code_logic.get('repository') != null){
    			response += code_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_logic" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_logic.get('error') != null){
    				if(code_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		$('#process_progress').html('');
    		
    	}});
    	
    	var response = "";
    	code_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(code_frontend.get('repository') != null){
    			response += code_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_frontend" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_frontend.get('error') != null){
    				if(code_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.WebsiteGeneratorView = Backbone.View.extend({

    tagName:"div", // Not required since 'div' is the default if no el or tagName specified

    initialize:function () {
        this.template = _.template(tpl.get('layouts/details_websitecodegenerator'));
        
        this.render();
        
    },

    events:{
        "click #btn_generate":"generateCode",
        "click #btn_deploy_database":"deployDatabase",
        "click #btn_deploy_api":"deployAPI",
        "click #btn_deploy_logic":"deployLogic",
        "click #btn_deploy_frontend":"deployFrontEnd",
        "click #btn_deploy_website":"deployWebsite"
    },
    
    generateCode:function () {
    	var code_website = new Code_Website({id: this.model.id});
    	
    	var progress_bar = '<div class="progress progress-striped active" id="process_progress">'+
    	'<div class="progress-bar" style="width: 100%;" id="process_progress"></div>'+
    	'</div>';
    	
    	$('#alerts').html('');
		
    	
    	$('#process_progress').html(progress_bar);
    	
    	var response = "";
    	code_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(code_website.get('repository') != null){
    			response += code_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span><button id="btn_deploy_website" name="singlebutton" class="btn btn-primary">deploy</button>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(code_website.get('error') != null){
    				if(code_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					code_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
        return false;
    },
    deployDatabase:function () {
    	var code_database = Code_DataBase.findOrCreate({id: this.model.id});
    	
    	var deployment_database = new Deployment_DataBase({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_database.fetch({success:function () {
    		$('#container_code_database').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_database.get('repository') != null){
    			response += deployment_database.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_database').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_database.get('error') != null){
    				if(deployment_database.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_database.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployAPI:function () {
    	var code_api = Code_API.findOrCreate({id: this.model.id});
    	
    	var deployment_api = new Deployment_API({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_api.fetch({success:function () {
    		$('#container_code_api').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_api.get('repository') != null){
    			response += deployment_api.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_api').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_api.get('error') != null){
    				if(deployment_api.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_api.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployLogic:function () {
    	var code_logic = Code_Logic.findOrCreate({id: this.model.id});
    	
    	var deployment_logic = new Deployment_Logic({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_logic.fetch({success:function () {
    		$('#container_code_logic').append("done");
    			
    		//$('#container_treeview').append('<p>' + fragments.models[i].get('text') + '</p>');
    		if(deployment_logic.get('repository') != null){
    			response += deployment_logic.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_logic').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        			
        			/*$(document).ready(function() {
        		        $('.tree').treegrid({
        		                    expanderExpandedClass: 'glyphicon glyphicon-minus',
        		                    expanderCollapsedClass: 'glyphicon glyphicon-plus'
        		                });
        		    });*/
        			 
        			
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_logic.get('error') != null){
    				if(deployment_logic.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_logic.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployFrontEnd:function () {
    	var code_frontend = Code_FrontEnd.findOrCreate({id: this.model.id});
    	
    	var deployment_frontend = new Deployment_FrontEnd({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_frontend.fetch({success:function () {
    		$('#container_code_frontend').append("done");
    			
    		if(deployment_frontend.get('repository') != null){
    			response += deployment_frontend.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_frontend').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_frontend.get('error') != null){
    				if(deployment_frontend.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_frontend.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    deployWebsite:function () {
    	var code_website = Code_Website.findOrCreate({id: this.model.id});
    	
    	var deployment_website = new Deployment_Website({id: this.model.id});
    	
    	
    	var response = "";
    	deployment_website.fetch({success:function () {
    		$('#container_code_website').append("done");
    			
    		if(deployment_website.get('repository') != null){
    			response += deployment_website.get('repository');
        		
        		if (response != 'null') {
        			$('#container_code_website').html('<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>');
        		} else {
        			var alert_msg = '<div class="alert alert-warning">'+
    				'Extraction Warning'+
    				'<br/>' + 'Missing Entity Retrieval'+
    		    	'</div>';
        			
        			$('#alerts').html(alert_msg);
        		}
        		
        		
        	} else {
    			if(deployment_website.get('error') != null){
    				if(deployment_website.get('error').get('details') != null) {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        				'<br/>' + extract.get('error').get('details')+
        		    	'</div>';
    				} else {
    					var alert_msg = '<div class="alert alert-danger">'+
    					deployment_website.get('error').get('message')+
        		    	'</div>';
    				}
    				
    				
    				$('#alerts').html(alert_msg);
    				
    			}
    		}
    		
    		
    		
    		$('#process_progress').html('');
    		
    	}});
    	
   	
        return false;
    },
    render: function () {
    	var data = {"data": this.model.toJSON()};
    	
         this.$el.html(this.template(data));
		

        return this;
    }
});
		
window.OntologyListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateOntology"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});
window.WebsiteListView = ObjectListView.extend({
	initialize : function(options) {
		this.options = options;
		
		this.template = _.template(tpl.get('layouts/objectlist'));
		
		this.tableView		= new BackGridTableView({collection : this.collection});
		this.tableView.columns.push({
			name : "actions", // The key of the model attribute
			label : "Actions", // The name to display in the header
			sortable: false,
			editable : false,
			cell : ActionsCell.extend({
				orderSeparator : '',
				actions : ["generateWebsite"]
			})
		})
	    this.buttonView 	= new ButtonView({id: "btn_add"});
	}
});